# The app is built so everything except drawing runs without a display, a sound
# card or libVLC — so the whole suite runs anywhere, including on the Pi itself.
#
#   pytest
[pytest]
testpaths = tests
addopts = -q
